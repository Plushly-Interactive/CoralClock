{
  "name": "reeflect",
  "private": true,
  "description": "Tooling only — the extension itself is unpacked and has no build step.",
  "scripts": {
    "capture": "node scripts/os/capture.mjs",
    "lint:i18n": "node .local/i18n-check.js ."
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
